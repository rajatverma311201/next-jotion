// https://direct-collie-4.clerk.accounts.dev

const authConfig = {
    // your auth config properties here
    providers: [
        {
            domain: "https://direct-collie-4.clerk.accounts.dev",
            applicationID: "convex",
        },
    ],
};

export default authConfig;
